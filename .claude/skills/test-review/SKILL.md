# /test-review — Redirected

This skill has been consolidated into `/test-create-review`.

Use:
- `/test-create-review create <layer> <target>` — to create new tests
- `/test-create-review review <layer> <target>` — to review and fix existing tests
- `/test-create-review audit` — to audit all tests without changes

See `/test-common` for mandatory rules.
