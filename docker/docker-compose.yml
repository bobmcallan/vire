name: vire

services:
  vire-mcp:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: vire-mcp
    ports:
      - "4242:4242"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:4242/sse"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
