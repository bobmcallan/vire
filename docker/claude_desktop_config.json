{
  "mcpServers": {
    "vire": {
      "command": "docker",
      "args": [
        "run", "--rm", "-i",
        "--network", "vire_default",
        "--entrypoint", "./vire-mcp",
        "vire-mcp:latest",
        "--stdio", "--server", "http://vire-server:4242"
      ]
    }
  }
}
