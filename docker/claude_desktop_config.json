{
  "mcpServers": {
    "vire": {
      "command": "docker",
      "args": [
        "run", "--rm", "-i",
        "--network", "vire_default",
        "-e", "VIRE_SERVER_URL=http://vire-server:4242",
        "vire-mcp:latest",
        "--stdio"
      ]
    }
  }
}
